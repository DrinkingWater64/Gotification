global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'gotification_api'
    static_configs:
      # This points to the homelab IP
      - targets: ['192.168.0.199:8080']
